import React from 'react';

import Page from '../components/Page';

export default function People({ history }){
    return (
        <Page page = { 'Person' } title = { 'PEOPLE' } history = { history }></Page>
    )
 }