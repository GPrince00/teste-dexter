import React from 'react';

import Page from '../components/Page';

export default function Places({ history }){
    return (
        <Page page = { 'Place' } title = { 'PLACES' } history = { history }></Page>
    )
 }